<Window  x:Class="StockApp.Net6.Views.Demo13MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockApp.Net6.Views" 
        xmlns:userControls="clr-namespace:StockApp.Net6.Views.UserControls"
        mc:Ignorable="d"
        Title="Demo13 - Observable Collection with ListView" Height="400" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtEntry" Height="35" FontSize="16" />
        <Button Grid.Column="1" x:Name="btnAdd" Width="50" Content="Add" Click="btnAdd_Click" HorizontalAlignment="Left" />
        <Button Grid.Column="2" x:Name="btnDelete" Width="50" Content="Delete" Click="btnDelete_Click" HorizontalAlignment="Left" Margin="60,0,0,0" />
        <Button Grid.Column="3" x:Name="btnClear" Width="50" Content="Clear" Click="btnClear_Click" HorizontalAlignment="Left" Margin="120,0,0,0" />

        <ListView x:Name="lvEntries" Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding Entries}"/>
    </Grid>
</Window>
